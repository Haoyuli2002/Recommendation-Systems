# 推荐算法白皮书

> 基于个人学习笔记整理，面向面试复习使用。

---

# 目录

- [1. 召回（Recall / Candidate Generation）](#1-召回recall--candidate-generation)
  - [1.1 协同过滤（Collaborative Filtering）](#11-协同过滤collaborative-filtering)
    - [1.1.1 ItemCF：基于物品的协同过滤](#111-itemcf基于物品的协同过滤)
    - [1.1.2 UserCF：基于用户的协同过滤](#112-usercf基于用户的协同过滤)
    - [1.1.3 Swing 模型](#113-swing-模型)
  - [1.2 矩阵补充（Matrix Completion）](#12-矩阵补充matrix-completion)
  - [1.3 双塔模型（Two-Tower Model）](#13-双塔模型two-tower-model)
  - [1.4 Deep Retrieval](#14-deep-retrieval)
  - [1.5 其他召回通道](#15-其他召回通道)
- [2. 粗排（Pre-Ranking）](#2-粗排pre-ranking)
  - [2.1 三塔模型](#21-三塔模型)
- [3. 精排（Ranking）](#3-精排ranking)
  - [3.1 多目标模型（Multi-Task Model）](#31-多目标模型multi-task-model)
  - [3.2 MMoE：Multi-gate Mixture-of-Experts](#32-mmoemulti-gate-mixture-of-experts)
  - [3.3 特征交叉模型](#33-特征交叉模型)
    - [3.3.1 FM：因式分解机](#331-fm因式分解机)
    - [3.3.2 DCN：Deep & Cross Network](#332-dcndeep--cross-network)
    - [3.3.3 LHUC / PPNet](#333-lhuc--ppnet)
    - [3.3.4 SENet](#334-senet)
    - [3.3.5 Bilinear Cross](#335-bilinear-cross)
  - [3.4 预估分数融合](#34-预估分数融合)
  - [3.5 视频播放建模](#35-视频播放建模)
  - [3.6 排序模型特征工程](#36-排序模型特征工程)
- [4. 用户行为序列建模（User Behavior Sequence Modeling）](#4-用户行为序列建模user-behavior-sequence-modeling)
  - [4.1 LastN 平均池化](#41-lastn-平均池化)
  - [4.2 DIN：Deep Interest Network](#42-dindeep-interest-network)
  - [4.3 SIM：Search-based Interest Model](#43-simsearch-based-interest-model)
- [5. 重排（Re-Ranking）](#5-重排re-ranking)
  - [5.1 物品相似度](#51-物品相似度)
  - [5.2 提高多样性的方法](#52-提高多样性的方法)
    - [5.2.1 MMR：Maximal Marginal Relevance](#521-mmrmaximal-marginal-relevance)
    - [5.2.2 滑动窗口](#522-滑动窗口)
  - [5.3 重排业务规则](#53-重排业务规则)
  - [5.4 DPP：行列式点过程](#54-dpp行列式点过程)
- [6. 冷启动（Cold Start）](#6-冷启动cold-start)
  - [6.1 UGC 冷启动概述](#61-ugc-冷启动概述)
  - [6.2 双塔模型冷启动改进](#62-双塔模型冷启动改进)
  - [6.3 类目召回通道](#63-类目召回通道)
  - [6.4 聚类召回](#64-聚类召回)
  - [6.5 Look-alike 人群扩散](#65-look-alike-人群扩散)
- [7. 附录](#7-附录)

---

# 1. 召回（Recall / Candidate Generation）

召回是推荐系统的第一阶段，从海量物品库（百万~亿级）中快速筛选出用户可能感兴趣的候选集（几千量级）。召回阶段的核心要求是**高效**和**高覆盖**，可以牺牲一定精度。通常采用多路召回策略，每条召回通道独立运行，结果取并集后送入排序阶段。

---

## 1.1 协同过滤（Collaborative Filtering）

> 核心思想：基于用户行为数据（点击、收藏、点赞等），利用"用户-物品"交互矩阵中的共现关系进行推荐，不依赖物品本身的内容或属性。

### 1.1.1 ItemCF：基于物品的协同过滤

**原理**

如果用户喜欢物品 $i_1$，而物品 $i_1$ 和物品 $i_2$ 相似（受众重合度高），那么推荐 $i_2$ 给用户。

**相似度计算**

- 喜欢物品 $i_1$ 的用户集合 $W_1$，喜欢物品 $i_2$ 的用户集合 $W_2$，交集 $V = W_1 \cap W_2$。
- 基础版（Jaccard 变体）：

$$
\text{sim}(i_1, i_2) = \frac{|V|}{\sqrt{|W_1| \cdot |W_2|}}
$$

- 考虑喜欢程度的版本（余弦相似度）：

$$
\text{sim}(i_1, i_2) = \frac{\sum_{v \in V} \text{like}(v, i_1) \cdot \text{like}(v, i_2)}{\sqrt{\sum_{u_1 \in W_1} \text{like}^2(u_1, i_1)} \cdot \sqrt{\sum_{u_2 \in W_2} \text{like}^2(u_2, i_2)}}
$$

**线上召回流程**

| 步骤 | 操作 | 描述 |
|------|------|------|
| 离线 | 用户→物品索引 | 记录用户最近交互过的物品 ID（last-n） |
| 离线 | 物品→物品索引 | 计算物品两两相似度，每个物品索引 top-k 相似物品 |
| 线上 | 召回 | 用户 → last-n 物品 → 每个物品的 top-k 相似物品 |
| 线上 | 打分 | $\text{score}(user, item) = \sum_j \text{like}(user, item_j) \times \text{sim}(item_j, item)$ |

**优缺点**

| 优点 | 缺点 |
|------|------|
| 实现简单，可解释性强 | 只基于行为数据，未利用物品属性 |
| 计算离线化，线上速度快 | 冷启动问题：新物品无交互数据 |
| 适合物品更新不频繁的场景 | 热门物品容易与所有物品相似（需降权） |

---

### 1.1.2 UserCF：基于用户的协同过滤

**原理**

如果用户 $u_1$ 和用户 $u_2$ 兴趣相似，而 $u_2$ 喜欢某物品，则推荐该物品给 $u_1$。

$$
\text{score}(user, item) = \sum_j \text{sim}(user, user_j) \times \text{like}(user_j, item)
$$

**用户相似度计算**

- 用户 $u_1$ 喜欢的物品集 $J_1$，用户 $u_2$ 喜欢的物品集 $J_2$，交集 $I = J_1 \cap J_2$。
- 基础版：

$$
\text{sim}(u_1, u_2) = \frac{|I|}{\sqrt{|J_1| \cdot |J_2|}}
$$

- 降低热门物品权重（IIF 思想）：热门物品人人都喜欢，无法体现个性化。

$$
\text{sim}(u_1, u_2) = \frac{\sum_{l \in I} \frac{1}{\log(1 + n_l)}}{\sqrt{|J_1| \cdot |J_2|}}
$$

其中 $n_l$ 是喜欢物品 $l$ 的用户数量。

**线上召回流程**

1. 用户 → top-k 相似用户（用户→用户索引）
2. top-k 用户 → 各自的 last-n 物品（用户→物品索引）
3. 汇总打分排序

**优缺点**

| 优点 | 缺点 |
|------|------|
| 能发现新兴趣（社交发现） | 用户量级大，相似度计算开销大 |
| 适合用户兴趣多样的场景 | 新用户冷启动问题 |
| 推荐结果多样性好 | 不如 ItemCF 稳定 |

**ItemCF vs UserCF**

| 维度 | ItemCF | UserCF |
|------|--------|--------|
| 适用场景 | 物品少、用户多（电商） | 用户少、物品多（新闻、社交） |
| 个性化 | 基于用户自身历史 | 基于相似群体 |
| 实时性 | 物品相似度离线算好，较稳定 | 用户兴趣变化快，需频繁更新 |

---

### 1.1.3 Swing 模型

**原理**

针对 ItemCF 中的"小圈子问题"（如微信群内互相转发导致虚假相似）和"泛用户问题"（大量随意浏览的用户带来的噪声），对用户对设置权重。

$$
\text{sim}(i_1, i_2) = \sum_{u_1 \in V} \sum_{u_2 \in V} \frac{1}{\alpha + \text{overlap}(u_1, u_2)}
$$

- $V$ 是同时喜欢 $i_1$ 和 $i_2$ 的用户集合
- $\text{overlap}(u_1, u_2)$ 是两个用户共同交互的物品数量
- 两用户交互重叠越大，说明可能在"小圈子"中，权重越低

**优缺点**

| 优点 | 缺点 |
|------|------|
| 有效缓解小圈子问题 | 计算复杂度比 ItemCF 高 |
| 对噪声用户有天然抵抗力 | 超参数 α 需要调优 |
| 工业界广泛使用（阿里等） | |

---

## 1.2 矩阵补充（Matrix Completion）

**原理**

将用户 ID 和物品 ID 通过 Embedding 层映射为向量，用向量内积预测用户对物品的兴趣分数。

- 数据集：$(u, i, y)$，其中 $y$ 是兴趣分数（0-4 分，涵盖曝光无点击到转发）
- 优化目标：

$$
\min_{A, B} \sum_{(u, i, y) \in \Omega} (y - \langle a_u, b_i \rangle)^2
$$

**优缺点**

| 优点 | 缺点 |
|------|------|
| 思路简单，易于理解 | 只用了 ID Embedding，未利用物品/用户属性 |
| | 负样本选取方式不合理（应区分未曝光和曝光未点击） |
| | 内积不如余弦相似度（未归一化） |
| | 回归损失不如交叉熵 |

> 矩阵补充可以看作双塔模型的简化版本，实际效果不如双塔模型。

---

## 1.3 双塔模型（Two-Tower Model）

**原理**

分别用两个独立的神经网络（塔）编码用户特征和物品特征，输出各自的表征向量，用余弦相似度衡量匹配程度。

- 用户塔输入：用户 ID、离散特征、连续特征
- 物品塔输入：物品 ID、离散特征、连续特征
- 两个塔各自输出一个向量 $a$（用户）和 $b$（物品），通过 $\cos(a, b)$ 计算相似度

**训练方式**

| 方式 | 描述 | 损失函数 |
|------|------|----------|
| Pointwise | 每个样本独立二分类 | 交叉熵 |
| Pairwise | 一正一负成对训练 | Triplet Hinge Loss: $L = \max\{0, \cos(a,b^-) + m - \cos(a,b^+)\}$ |
| Listwise | 一正多负列表训练 | Softmax + 交叉熵 |

**正负样本选择**

| 类型 | 策略 |
|------|------|
| 正样本 | 曝光且点击的物品；过采样冷门物品，降采样热门物品 |
| 简单负样本 | 未被召回的物品；非均匀采样，概率 ∝ 点击次数^0.75 |
| Batch 内负样本 | 同 batch 内其他用户的正样本作为当前用户的负样本 |
| 困难负样本 | 被召回但被粗排/精排淘汰的物品 |

**上线部署**

| 阶段 | 操作 |
|------|------|
| 离线 | 用物品塔预计算所有物品 embedding，建立 ANN 索引（如 FAISS） |
| 线上 | 用户发起请求 → 用户塔实时计算用户向量 → 最近邻检索 |

> 用户兴趣动态变化，需要在线推理；物品特征相对稳定，可以缓存。

**模型更新**

| 方式 | 描述 |
|------|------|
| 全量更新 | 每天凌晨用前一天数据训练 1 epoch（基于昨日模型参数，非随机初始化） |
| 增量更新 | Online Learning，实时收集数据，流式更新 ID Embedding 参数 |

**优缺点**

| 优点 | 缺点 |
|------|------|
| 利用丰富的用户/物品特征 | 用户和物品特征独立编码，无法建模交叉特征 |
| 物品 embedding 可离线计算，线上仅需推理用户塔 | 模型表达能力受限于向量内积/余弦相似度 |
| 支持 ANN 高效检索 | 负样本选择策略对效果影响很大 |
| 工业界最主流的召回模型 | |

---

## 1.4 Deep Retrieval

**原理**

不同于双塔模型用向量检索，Deep Retrieval 用"路径"来表示物品，通过神经网络预测用户对路径的兴趣。

- 物品表示：每个物品映射到若干条路径，一条路径由多层节点组成，如 path = [a, b, c]
- 预测模型：逐层预测用户对节点的兴趣

$$
p(a, b, c | x) = p_1(a | x) \times p_2(b | a; x) \times p_3(c | a, b; x)
$$

**线上召回**

1. 给定用户特征 $x$，用 Beam Search 在路径树上搜索分数最高的 $s$ 条路径
2. 通过路径→物品索引，找到对应物品

**训练**

| 阶段 | 目标 |
|------|------|
| 学习神经网络参数 | 如果用户点击了物品，更新网络使对应路径分数变大 |
| 学习物品表征 | 根据用户兴趣分数确定物品与路径的对应关系，同时控制每条路径上的物品数量 |

**优缺点**

| 优点 | 缺点 |
|------|------|
| 不依赖 ANN 索引，路径搜索效率高 | 模型结构复杂，训练难度大 |
| 物品路径可以灵活更新 | 路径层数和宽度需要调优 |
| 适合超大规模物品库 | Beam Search 可能遗漏最优路径 |

---

## 1.5 其他召回通道

| 召回通道 | 索引结构 | 是否个性化 | 特点 |
|----------|----------|-----------|------|
| GeoHash 地理位置召回 | GeoHash → 优质笔记（按时间倒排） | ❌ | 推荐附近内容 |
| 同城召回 | 城市 → 优质笔记 | ❌ | 粒度更粗的地理召回 |
| 关注作者召回 | 用户→关注作者→最新笔记 | ✅ | 社交关系驱动 |
| 有交互作者召回 | 用户→有交互作者→最新笔记 | ✅ | 隐式兴趣 |
| 相似作者召回 | 用户→感兴趣作者→相似作者→最新笔记 | ✅ | 扩展发现 |
| 缓存召回 | 精排高分但未被重排选中的笔记 | ✅ | 避免浪费精排结果 |

**曝光过滤：Bloom Filter**

- 用于判断物品是否已对用户曝光过，避免重复推荐
- k 个哈希函数，m bits 哈希表，n 个物品
- 误伤概率：

$$
\left(1 - \exp\left(-\frac{kn}{m}\right)\right)^k
$$

- 判断为 No → 一定不在集合中；判断为 Yes → 可能在集合中（存在误判）

---

# 2. 粗排（Pre-Ranking）

粗排是召回和精排之间的中间环节。召回输出几千个候选物品，精排模型复杂但计算开销大，无法处理全部候选。粗排用较轻量的模型从中筛选出几百个候选送入精排。

## 2.1 三塔模型

**架构**

| 塔 | 输入特征 | 特点 |
|----|----------|------|
| 用户塔（较大） | 用户特征 + 场景特征 | 每个请求推理 1 次 |
| 物品塔（较大） | 物品特征（相对静态） | 可缓存输出向量，避免绝大部分推理 |
| 交叉塔（较小） | 统计特征 + 交叉特征 | 每个候选物品推理 1 次 |

**推理流程**

1. 用户塔：给定 1 个用户 → 推理 1 次
2. 物品塔：n 个召回物品 → 先查缓存，未命中才推理
3. 交叉塔：n 个物品 → n 次推理
4. 三个塔的输出融合，送入上层网络，给 n 个物品打分

**优缺点**

| 优点 | 缺点 |
|------|------|
| 物品塔可缓存，大幅降低计算量 | 三塔独立编码，交叉能力弱于精排 |
| 比精排轻量，比双塔（召回）更精确 | 交叉塔仍需 n 次推理 |
| 架构灵活，可以调整各塔大小 | |

---

# 3. 精排（Ranking）

精排是推荐系统中模型最复杂、特征最丰富的阶段，对粗排输出的几百个候选进行精细打分和排序。

## 3.1 多目标模型（Multi-Task Model）

**原理**

对每个候选物品，同时预测多个业务指标的概率（点击率、点赞率、收藏率、转发率等），将预估分数融合后排序。

**输入特征**

| 类型 | 示例 |
|------|------|
| 用户特征 | 用户 ID、性别、年龄、兴趣标签 |
| 物品特征 | 物品 ID、类目、关键词、发布时间 |
| 统计特征 | 用户/物品近 30 天曝光数、点击数、点赞数 |
| 场景特征 | 请求时间、地点、设备信息 |

**模型结构**

特征拼接 → 共享底层网络（Shared Bottom） → 输出向量 → 多个任务头（各自 FC + Sigmoid）→ 输出各业务指标概率

**训练**

- 每个任务头是二分类（是否点击/点赞/收藏/转发）
- 损失函数：加权交叉熵之和

$$
\text{Loss} = \sum_{i=1}^{4} \alpha_i \cdot \text{CrossEntropy}(y_i, p_i)
$$

- 类别不平衡处理：负样本降采样，然后用校准公式修正预估值

**优缺点**

| 优点 | 缺点 |
|------|------|
| 多任务共享底层，节省计算 | 任务间可能存在冲突（跷跷板效应） |
| 统一建模，方便维护 | 共享底层可能限制各任务的学习 |

---

## 3.2 MMoE：Multi-gate Mixture-of-Experts

**原理**

改进 Shared Bottom 结构，使用多个 Expert 网络（结构相同但参数不同），每个任务有独立的 Gate Network 来决定各 Expert 的贡献权重。

**结构**

1. 输入特征送入 m 个 Expert 网络，各输出一个向量
2. 每个任务有一个 Gate Network，输入相同特征，输出 m 个权重（Softmax）
3. 每个任务的输入 = Expert 输出的加权平均
4. 加权平均结果送入任务头进行预测

**极化问题（Polarization）**

- Softmax 输出可能接近 one-hot，导致只使用单个 Expert，浪费其他 Expert 的参数
- 解决方案：对 Softmax 输出做 Dropout（每个 Expert 有 10% 概率被丢弃）

**优缺点**

| 优点 | 缺点 |
|------|------|
| 各任务可以学到不同的 Expert 组合 | 可能出现极化问题 |
| 缓解任务间的冲突 | 比 Shared Bottom 参数量更大 |
| 工业界广泛使用 | Gate Network 设计需要调优 |

---

## 3.3 特征交叉模型

> 目的：自动学习特征之间的组合关系，提升模型表达能力。

### 3.3.1 FM：因式分解机

**原理**

在线性模型基础上加入二阶特征交叉项，并用低秩矩阵分解降低参数量。

- 线性模型：$p = b + \sum_{i=1}^{d} w_i x_i$，参数量 $O(d)$
- 加入交叉项：$p = b + \sum_i w_i x_i + \sum_{i=1}^{d} \sum_{j=i+1}^{d} u_{ij} x_i x_j$，参数量 $O(d^2)$
- FM 用低秩分解：$u_{ij} \approx \langle v_i, v_j \rangle$，参数量降至 $O(kd)$（k 为向量维度）

**优缺点**

| 优点 | 缺点 |
|------|------|
| 参数量从 $O(d^2)$ 降到 $O(kd)$ | 只能建模二阶交叉 |
| 稀疏数据下也能有效学习 | 高阶交叉需要扩展（如 DeepFM） |

### 3.3.2 DCN：Deep & Cross Network

**原理**

设计专门的 Cross Layer，显式建模高阶特征交叉。

- Cross Layer 公式：

$$
x_{i+1} = x_0 \odot (W \cdot x_i + b) + x_i
$$

- $x_0$ 是原始特征，$\odot$ 是逐元素相乘（Hadamard product）
- 每一层 Cross Layer 增加一阶交叉，L 层可以建模 L+1 阶交叉

**网络结构**

输入特征同时送入 Cross Network（多层 Cross Layer）和 Deep Network（普通 DNN），两部分输出拼接后做最终预测。

**优缺点**

| 优点 | 缺点 |
|------|------|
| 显式建模高阶交叉，可控性好 | Cross Layer 的表达能力有理论上界 |
| 参数高效（每层只有 W 和 b） | 实际效果取决于数据特点 |
| 与 DNN 互补 | |

### 3.3.3 LHUC / PPNet

**原理**

Learning Hidden Unit Contribution（LHUC），快手用于精排时称为 PPNet。

- 核心思想：用用户特征通过一个小型神经网络（多层 FC + 2×Sigmoid）生成"门控信号"
- 门控信号对主网络的隐藏层进行逐元素缩放，实现个性化的特征放大/抑制

**优缺点**

| 优点 | 缺点 |
|------|------|
| 提高个性化程度 | 增加了网络参数和推理时间 |
| 实现简单，即插即用 | 效果依赖于用户特征的质量 |

### 3.3.4 SENet

**原理**

对离散特征进行 field-wise（字段级别）加权，学习每个特征 field 的重要性。

**流程**

1. m 个特征，每个是 k 维向量 → $m \times k$ 矩阵
2. Average Pooling → $m \times 1$ 向量
3. FC + ReLU 降维到 $\frac{m}{r} \times 1$（r 为压缩比，通常 4 或 8）
4. FC + Sigmoid 升维到 $m \times 1$
5. 与原始矩阵逐行相乘（field-wise reweighting）

**优缺点**

| 优点 | 缺点 |
|------|------|
| 自动学习特征重要性 | 需要所有特征 embedding 维度一致 |
| 轻量级，计算开销小 | 只做 field-wise 加权，不做交叉 |
| 与其他交叉方法可叠加使用 | |

### 3.3.5 Bilinear Cross

**原理**

用可学习的矩阵 $W$ 建模特征之间的交互，比简单内积更有表达力。

| 方式 | 公式 | 输出 |
|------|------|------|
| Bilinear 内积 | $e_i^T W e_j$ | 标量 |
| Bilinear 哈达玛乘积 | $e_i \odot (W e_j)$ | 向量 |

**优缺点**

| 优点 | 缺点 |
|------|------|
| 比内积表达力更强 | 参数矩阵 $W$ 增加存储和计算 |
| 可以捕获非对称关系 | 特征组合数多时计算量大 |

---

## 3.4 预估分数融合

多目标模型输出多个预估分数后，需要融合为一个排序分数。常见方案：

| 方案 | 公式 | 适用场景 |
|------|------|----------|
| 简单加权和 | $p_{click} + w_1 \cdot p_{like} + w_2 \cdot p_{collect} + \ldots$ | 通用 |
| 点击率乘加权和 | $p_{click} \cdot (1 + w_1 \cdot p_{like} + w_2 \cdot p_{collect} + \ldots)$ | 点击是核心指标 |
| 幂次乘积（短视频） | $(1 + w_1 \cdot p_{time})^{\alpha_1} \cdot (1 + w_2 \cdot p_{like})^{\alpha_2} \cdot \ldots$ | 短视频 |
| 排名融合（快手） | $\sum_i \frac{1}{r_i^{\alpha_i} + \beta_i}$（r 为各指标排名） | 多指标均衡 |
| 漏斗概率（电商） | $p_{click}^{\alpha_1} \times p_{cart}^{\alpha_2} \times p_{pay}^{\alpha_3} \times price^{\alpha_4}$ | 电商转化漏斗 |

---

## 3.5 视频播放建模

### 播放时长

- 直接回归效果不好
- 解决方案：将播放时长离散化后用交叉熵优化

### 完播率

| 方法 | 描述 |
|------|------|
| 回归 | 完播率 $y \in [0,1]$，用交叉熵优化：$\text{loss} = y \cdot \log p + (1-y) \cdot \log(1-p)$ |
| 二分类 | 播放超过 80% 定义为完播，做 0/1 分类 |

> 注意：完播率不能直接用于融分，因为短视频天然完播率高于长视频。需要做归一化：$p_{finish} = \frac{\text{预估完播率}}{f(\text{视频时长})}$

---

## 3.6 排序模型特征工程

### 特征分类

| 类别 | 特征项 |
|------|--------|
| 用户画像 | 用户 ID、性别、年龄、新老用户、活跃度、兴趣类目/关键词/品牌 |
| 物品画像 | 物品 ID、发布时间、GeoHash/城市、标题/类目/关键词、内容质量（字数、图片数、美学分等） |
| 用户统计特征 | 近 30 天曝光/点击/点赞/收藏数（按图文/视频、按类目分桶统计） |
| 物品统计特征 | 近 30 天曝光/点击/点赞等（按用户性别、年龄分桶统计） |
| 场景特征 | 用户定位、当前时刻、是否节假日、手机品牌/型号/OS |

### 特征处理

| 特征类型 | 处理方式 |
|----------|----------|
| 离散特征（ID、类目、城市等） | Embedding |
| 连续特征（年龄、字数、时长等） | 分桶离散化 |
| 长尾连续特征（曝光量等） | $\log(1 + x)$ 平滑 |

---

# 4. 用户行为序列建模（User Behavior Sequence Modeling）

用户行为序列是推荐系统中最重要的特征之一。通过建模用户过去交互过的物品序列，可以捕捉用户的兴趣偏好。不同模型在序列长度、计算复杂度和适用阶段上有所不同。

---

## 4.1 LastN 平均池化

**原理**

取用户最近交互（点赞、收藏等）的 n 个物品 ID，通过 Embedding 层得到 n 个向量，取平均作为用户兴趣表征。

**适用阶段**

- ✅ 召回（双塔模型）：LastN 平均向量作为用户塔的输入特征
- ✅ 粗排（三塔模型）：同上
- ✅ 精排模型：作为用户特征输入

**优缺点**

| 优点 | 缺点 |
|------|------|
| 实现简单，计算量小 | 所有历史物品等权重，无法区分重要性 |
| 只依赖用户自身特征，适用于所有阶段 | 平均操作会稀释重要信号 |
| 无需候选物品信息 | n 较小时只能捕捉短期兴趣 |

---

## 4.2 DIN：Deep Interest Network

**原理**

用注意力机制替代简单平均，根据候选物品与 LastN 中每个物品的相关性分配不同权重。

- 给定候选物品 embedding $e_{candidate}$ 和 LastN 物品 embedding $e_1, e_2, \ldots, e_n$
- 计算注意力权重：$\alpha_i = \text{Attention}(e_{candidate}, e_i)$
- 加权平均：$\text{user\_interest} = \sum_{i=1}^{n} \alpha_i \cdot e_i$

**关键特点**

- 注意力权重依赖候选物品，因此**只适用于精排**（召回/粗排阶段无法获取候选物品信息）
- n 通常为几百，关注用户的**短期兴趣**

**优缺点**

| 优点 | 缺点 |
|------|------|
| 针对不同候选物品激活不同的用户兴趣 | 需要候选物品信息，不能用于召回/粗排 |
| 比平均池化更精确 | n 较大时注意力计算开销大 |
| 工业界广泛使用（阿里原创） | 只能建模短期兴趣（n 受限于几百） |

---

## 4.3 SIM：Search-based Interest Model

**原理**

解决 DIN 无法处理长行为序列的问题。保留用户的完整长期行为序列（n 可达上万），但通过**先检索再注意力**的两阶段策略控制计算量。

**流程**

1. **检索阶段**：从 LastN（n 很大）中快速找到与候选物品最相关的 k 个物品（k << n）
2. **注意力阶段**：对 TopK 物品用 DIN 的注意力机制计算加权表征

**检索方式**

| 方式 | 描述 | 特点 |
|------|------|------|
| Hard Search | 按候选物品的类目筛选，保留 LastN 中类目相同的物品 | 简单快速，无需训练 |
| Soft Search | 用物品 embedding 做 k 近邻检索 | 更精确，需要 ANN 索引 |

**时间信息建模**

- SIM 的序列覆盖长期行为，时间跨度大，**时间信息很重要**
- 用户与 LastN 物品交互的时间差 $\delta$ → 离散化 → Embedding → 与物品向量拼接
- DIN 不需要时间信息，因为其序列短，都是近期行为

**优缺点**

| 优点 | 缺点 |
|------|------|
| 支持超长行为序列（上万级） | 检索阶段引入额外工程复杂度 |
| 兼顾长期兴趣和计算效率 | Hard Search 可能漏掉跨类目的相关物品 |
| 时间衰减建模更合理 | Soft Search 需要维护 ANN 索引 |
| | 只适用于精排 |

**三种方法对比**

| 维度 | LastN 平均 | DIN | SIM |
|------|-----------|-----|-----|
| 序列长度 | 短（几十~几百） | 短（几百） | 长（上万） |
| 兴趣类型 | 短期 | 短期 | 长期 + 短期 |
| 是否需要候选物品 | ❌ | ✅ | ✅ |
| 适用阶段 | 召回/粗排/精排 | 精排 | 精排 |
| 计算复杂度 | 低 | 中 | 中（检索 + 注意力） |
| 个性化程度 | 低 | 高 | 高 |

---

# 5. 重排（Re-Ranking）

重排是推荐系统的最后一个环节，对精排输出的候选列表进行后处理，核心目标是在保持相关性的同时**提高多样性**，并满足业务规则约束。

---

## 5.1 物品相似度

重排需要度量物品之间的相似度，以便在选择结果时兼顾多样性。

### 基于物品的属性标签

- 类目、品牌、关键词
- 根据一级类目、二级类目、品牌计算相似度

### 基于物品的向量表征

1. **召回阶段双塔模型的物品塔中的物品表征**
    - 效果一般
    - 头部现象：曝光和点击集中在少数物品，长尾物品和新物品上表现差
2. **基于图文内容的物品表征**
    - CV、NLP
    - 用 CNN 提取图像特征，用 BERT 提取文本特征
    - CLIP 来进行图片和文本的 embedding space 对齐
        - 对于图片-文本二元组，预测图文是否匹配

---

## 5.2 提高多样性的方法

从 n 个候选中选出 k 个，既保证总分高，也需要保证多样性。

### 5.2.1 MMR：Maximal Marginal Relevance

**原理**

精排给 n 个物品打分，融合分数为 $reward_1, \ldots, reward_n$。把第 i 个物品和第 j 个物品的相似度记作 $sim(i,j)$。

- 被选中物品集合：$S$
- 未被选中物品集合：$R$
- 计算集合 $R$ 中每个物品 $i$ 的 Marginal Relevance：

$$
MR_i = \theta \cdot reward_i - (1-\theta) \cdot \max_{j \in S} sim(i,j)
$$

**贪心算法流程**

1. 初始化 $S = \emptyset$，$R = \{$全部候选$\}$
2. 每一步从 $R$ 中选出 $MR$ 最大的物品 $i^* = \argmax_{i \in R} MR_i$
3. 将 $i^*$ 从 $R$ 移入 $S$
4. 重复步骤 2-3，直到 $|S| = k$

**超参数 $\theta$**

- $\theta$ 接近 1：更重视精排分数（相关性优先）
- $\theta$ 接近 0：更重视多样性

### 5.2.2 滑动窗口

在已排好的列表上用滑动窗口检查，如果窗口内物品相似度过高（如同类目过多），则进行位置调换，确保局部多样性。

---

## 5.3 重排业务规则

| 规则 | 描述 |
|------|------|
| 连续限制 | 最多连续出现 k 篇某种笔记 |
| 间隔限制 | 每 k 篇笔记最多出现 1 篇某种笔记（运营、广告） |
| 头部限制 | 前 t 篇笔记最多出现 k 篇某种笔记 |

---

## 5.4 DPP：行列式点过程

**原理**

DPP（Determinantal Point Process）用行列式来衡量子集的"质量"，天然地同时编码了**相关性**和**多样性**。

- 构建核矩阵 $L$，其中 $L_{ij} = q_i \cdot \text{sim}(i,j) \cdot q_j$
  - $q_i$ 是物品 $i$ 的精排分数（质量）
  - $\text{sim}(i,j)$ 是物品 $i$ 和 $j$ 的相似度
- 对于子集 $S$，其"质量"为 $\det(L_S)$（$L_S$ 是 $L$ 的子矩阵）
- **行列式的性质**：
  - 对角线元素 $L_{ii} = q_i^2$：物品质量越高，行列式越大
  - 非对角线元素越大（物品越相似），行列式越小
  - 因此 $\det(L_S)$ 天然平衡了质量和多样性

**目标**

$$
S^* = \argmax_{S, |S|=k} \det(L_S)
$$

精确求解是 NP-hard 问题，实际中用贪心算法近似求解。

**MMR vs DPP 对比**

| 维度 | MMR | DPP |
|------|-----|-----|
| 方法论 | 贪心逐步选择 | 行列式建模子集质量 |
| 多样性度量 | 与已选集合中最相似物品的相似度 | 子集内所有物品的整体多样性 |
| 理论基础 | 启发式 | 概率论（行列式点过程） |
| 计算复杂度 | $O(k \cdot n)$ | $O(k^2 \cdot n)$（贪心近似） |
| 工业应用 | 广泛 | 广泛（抖音、小红书等） |

---

# 6. 冷启动（Cold Start）

冷启动是推荐系统面临的关键挑战之一。当新物品或新用户缺乏交互数据时，传统的协同过滤和基于行为的模型无法有效工作。

---

## 6.1 UGC 冷启动概述

> UGC（User-Generated Content）：用户生成内容，如小红书笔记、B 站视频、头条文章。

新笔记刚刚发布，缺少和用户的交互，导致推荐的难度大、效果差。需要扶持新发布、低曝光的文章，来增长作者发布意愿。

**目标**

1. **精准推荐**：把新笔记推荐给合适的用户，不引起用户的反感
2. **激励发布**：流量向低曝光新笔记倾斜，激励作者发布
3. **挖掘高潜**：通过初期的小流量试探，找到高质量的笔记，给予流量倾斜

**评价指标**

| 维度 | 指标 | 说明 |
|------|------|------|
| 作者侧 | 发布渗透率 | $\frac{\text{当日发布人数}}{\text{日活人数}}$ |
| 作者侧 | 人均发布量 | $\frac{\text{当日发布笔记数}}{\text{日活人数}}$ |
| 用户侧 | 新笔记指标 | 新笔记的点击率、交互率 |
| 用户侧 | 大盘指标 | 日活、月活等消费指标 |
| 内容侧 | 高热笔记占比 | 优质内容的发现效率 |

> 注意：大力扶持低曝光新笔记，作者侧指标会变好，但用户侧大盘消费指标可能变差。需要平衡。

---

## 6.2 双塔模型冷启动改进

新笔记的挑战：
- 没有用户点击、点赞 → 没法用 ItemCF
- 没有笔记 ID Embedding（或 ID Embedding 学不好）

**改进策略**

| 策略 | 描述 |
|------|------|
| Default Embedding | 所有新笔记共享一个默认 ID，而不是用自己真正的 ID |
| 相似笔记向量 | 查找 Top-K 内容最相似的高曝光笔记，取其 embedding 平均值 |
| 多召回池 | 设置 1 小时、6 小时、24 小时、30 天新笔记等多个召回池 |

---

## 6.3 类目召回通道

**原理**

利用用户画像中的兴趣类目和关键词，直接匹配新笔记的类目标签。

- 用户画像：感兴趣的类目（美食、科技数码）、关键词（大模型、布偶猫）
- 新笔记带有类目和关键词标签
- 按类目匹配召回

**优缺点**

| 优点 | 缺点 |
|------|------|
| 实现简单，不依赖交互数据 | 弱个性化，不够准确 |
| 对新笔记友好 | 新笔记发布几小时后就无法被召回 |

---

## 6.4 聚类召回

**原理**

如果用户喜欢一个笔记，那么他会喜欢内容相似的笔记。

**离线准备**

1. 训练一个神经网络，基于笔记的类目和图文内容，把笔记映射到向量
   - CNN 提取图片特征 + BERT 提取文本特征 → 拼接 → MLP → 特征向量
2. 对笔记向量做 k-means 聚类（余弦相似度），划分为 1000 个 cluster
3. 记录每个 cluster 的中心方向
4. 建立索引：cluster → 笔记 ID 列表（按时间倒排）

**新笔记入库**

1. 新笔记发布时，用神经网络映射到特征向量
2. 从 1000 个 cluster 的 centroid 向量中寻找最相似的 cluster
3. 加入该 cluster 的索引列表

**线上召回**

1. 给定用户 ID，找到其 last-n 交互的笔记列表（种子笔记）
2. 把每个种子笔记映射到向量，寻找最相似的 cluster
3. 从每个 cluster 的索引列表中，取回最新的 m 篇笔记
4. 最多取回 $m \times n$ 篇笔记

**内容相似度模型训练**

- 正样本：同一用户点击的两篇笔记
- 负样本：随机采样的不相关笔记
- 训练目标：使相似笔记的向量距离更近

---

## 6.5 Look-alike 人群扩散

**原理**

找到对新笔记感兴趣的种子用户群体，然后扩散到相似的更大用户群体。

1. 新笔记获得少量初始曝光，收集正向交互用户作为种子用户
2. 通过用户相似度模型，找到与种子用户相似的更多用户
3. 将新笔记推荐给这些相似用户

---

# 7. 附录

### 离散特征的向量化

| 方法 | 描述 | 适用场景 |
|------|------|----------|
| One-Hot | 类别数 = 向量维度，稀疏 | 类别数少（如性别） |
| Embedding | 类别序号 → 低维稠密向量，参数量 = 维度 × 类别数 | 类别数多（ID、关键词等） |

### 系统全链路

```
用户请求 → 多路召回（千级候选）→ 粗排（百级候选）→ 精排（百级候选打分）→ 重排（多样性+规则）→ 展示（十级）